name: Linting for evendemy

on: push 

jobs: 
    unit-tests-backend:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - run: |
                echo "I don't know what to do the linting with".