{
  "name": "evendemy-server",
  "version": "2.0.0-beta.3",
  "license": "GNU General Public License v3.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "npm run build-core && npm run build-default-files",
    "build-core": "webpack --c webpack.config.js && cp package.json dist/package.json && cp package-lock.json dist/package-lock.json",
    "build-default-files": "cp -R assets dist && cp config.json dist/config.json && cp -R plugins dist"
  },
  "author": "",
  "devDependencies": {
    "webpack": "^4.17.1",
    "webpack-cli": "^3.1.0"
  },
  "dependencies": {
    "basic-auth": "^1.1.0",
    "ical-generator": "^0.2.9",
    "ldapauth-fork": "^3.0.0",
    "moment": "^2.21.0",
    "mongoose": "^4.7.6",
    "mongoose-auto-increment": "^5.0.1",
    "mustache": "^2.3.0",
    "nodemailer": "^2.7.0",
    "restify": "^4.3.0",
    "restify-cors-middleware": "0.0.8",
    "underscore": "^1.8.3"
  }
}
